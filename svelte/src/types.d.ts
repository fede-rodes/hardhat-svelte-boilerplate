/**
 * This file is a script as it does not import and export anything
 * (unlike module), making its declarations ambient - available globally.
 * Just make sure it is included in the tsconfig.json file in the
 * include/blob array "include": ["..."].
 */
type Address = `0x${string}`;
